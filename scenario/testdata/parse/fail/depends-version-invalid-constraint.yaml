name: depends-version-invalid-constraint
description: a scenario with invalid constraint specified in dependencies version
depends:
  - name: ls
    version:
      constraint: invalidconstraint
tests:
  - name: should-not-get-here
    foo: baz
